[project]
name = "meridian"
version = "0.1.0"
description = "Customer support assistant for Meridian"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=6.13.0",
    "openai-agents>=0.14.8",
    "python-dotenv>=1.2.2",
]
